"""Deliberative agents — those that build and reason with an internal world model.

This package contains the Latent-World-Model + SAC architecture used for the
continuous (Gaussian) and discrete (Categorical) world-model agents in the
DIA project. Both agents share the same skeleton; only the latent distribution
of the encoder and dynamics modules differs.
"""
